"""Regression tests for DCS RadioForge.

Run from the repository root with the project venv:

    .venv/Scripts/python.exe -m unittest discover -s tests -t . -v

Network tests (real edge-tts + ffmpeg) are skipped unless RF_NETWORK_TESTS=1.
"""
